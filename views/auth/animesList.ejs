<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= user.username %>'s Anime List</title>
</head>
<body>
  <h1><%= user.username %>'s Anime List</h1>
  <a href="/auth/animesList/addAnime">
    <button>Add anime</button>
  </a>

  <% if (animeList.length > 0) { %>
    <% animeList.forEach(anime => { %>
      <div class="anime-entry">
        <a href="/auth/animesList/<%= anime._id %>">
            <h1><%= anime.name %></h1>
          </a>
        <p>Rating: <%= anime.rating %></p>
        <p>Description: <%= anime.description %></p>
        <% if (anime.image) { %>
          <img src="<%= anime.image %>" alt="<%= anime.name %> image" style="max-width: 200px;"/>
        <% } %>
      </div>
    <% }) %>
  <% } else { %>
    <p>You haven't added any anime yet. Start by clicking "Add anime"!</p>
  <% } %>
</body>
</html>